namespace VeiculosProject
{
    class Program
    {
        static void Main(string[] args)
        {
           // Program.EscreverVeiculos();
        }

        static void EscreverVeiculos()
        {
            //IEnumerable<Veiculo> lista = Program.Veiculos();
            //lista.ToList().ForEach(x => x.Mover());
            //lista.ToList().ForEach(x => x.Parar());
            //List<string> listaDeTipos = lista.Select(x => x.GetType().ToStrinf().ToLower().Replace)
        }
    }

}
